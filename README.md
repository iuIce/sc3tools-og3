sc3tools for Oregairu Kan, making it easier to add double half-width characters as a single full-width character to the charset.
The original charset ends at line 7552, any new characters must also be added to the bitmap fonts (specifically font2.png for choices).

The <vtext> tag will be replaced with [linebreak] when replacing text in the .msb files.